/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ❌ sin basePath
  // ❌ sin redirect desde '/' (queremos mantener la página de construcción)
};

export default nextConfig;
